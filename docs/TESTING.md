# 0.7.0 验证记录

- TypeScript 类型检查通过。
- 32 项核心测试通过，覆盖数据迁移、备份、检索、来源变更和请求取消等行为。
- Android ARM64 Release 构建通过，包名 app.zixu.memory，versionCode 29。
- 构建的 Metro 源码映射与工作区 Main.tsx 一致，APK 内 bundle 哈希与该次构建产物一致。
- 回忆卡片网页预览已检查；网页预览不能替代 Android 真机验收。
- 新功能交互与 320/393/620 宽度的网页布局回归通过。
- 用户已确认 0.7.0 的目标布局调整可用。未开展完整的跨设备真机测试。

## 仍需验证

真实软键盘与系统大字体、录音中断、权限拒绝、跨设备恢复、真实供应商计费调用和长期大数据量性能。

测试脚本使用虚构数据和模拟接口，不应放入真实用户记忆或 API Key。
