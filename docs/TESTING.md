# 0.8.2 验证记录

- TypeScript 类型检查通过。
- 40 项核心测试通过，覆盖数据迁移、备份、检索、来源变更、请求取消、主题色调色板契约及导入冲突摘要。
- Android ARM64 Release 构建通过，包名 app.zixu.memory，versionName 0.8.2、versionCode 32，targetSdk 36，仅 arm64-v8a。
- 构建副本按文件相对路径同步并逐个核验 SHA-256，共 56 个文件；Metro 源码映射中 23 个应用模块与工作区源码一致；APK 内 `index.android.bundle` 与该次构建产物一致；签名证书与历史 0.8.0 APK 相同，可覆盖安装。
- 主题色、布局与增强三套网页回归通过；八套核心网页冒烟也已修复并通过，包括首次引导、模拟 AI、附件、个人认识、草稿失败和 2,001 条记录可靠性流程。设置页滚动位置和价值观反例摘要回归通过。网页预览不能替代 Android 真机验收。
- 未开展跨设备真机测试，也未验收覆盖安装后的偏好保留。

## 仍需验证

真实软键盘与系统大字体（含色块行在 320 宽度真机上的排布）、录音中断、权限拒绝、跨设备恢复、真实供应商计费调用和长期大数据量性能。主题色在安卓端的实际渲染，以及跟随系统在厂商 ROM 上的表现。

## 网页脚本状态

已运行并通过 `smoke.cjs`、`ai-smoke.cjs`、`interaction-smoke.cjs`、`attachments-smoke.cjs`、`values-smoke.cjs`、`self-smoke.cjs`、`draft-failure-smoke.cjs`、`reliability-smoke.cjs`、`layout-smoke.cjs`、`enhancements-smoke.cjs`、`accent-smoke.cjs` 与 `settings-scroll-smoke.cjs`。主 smoke 在空白浏览器状态下验证首次引导提示并真实点击「开始记录」；其余脚本也真实完成引导后再准备各自的虚构测试数据。AI 接口均由脚本拦截，不会调用真实供应商。设置滚动回归覆盖四轮离开/重开、返回与示例退出、浅深色、窄屏、改变 viewport 和输入框聚焦；价值观回归覆盖两条反例、长文本、320px 窄屏和点开原记录。

导入冲突摘要现在按记录编号建立索引；范围 ID 筛选使用集合。核心测试覆盖相同编号相同内容、相同编号不同内容及无冲突。网页预览不能替代 Android 真机验收，也不能证明大数据量在真机上的性能。

测试脚本使用虚构数据和模拟接口，不应放入真实用户记忆或 API Key。

可靠性回归在点击「选择备份并恢复」前先注册 `filechooser` 事件，再与真实点击配对等待；旧的 2 秒固定等待可能在控件有大量测试数据时早于真实事件触发。产品侧的用户手势修复是同步调用文件选择后再更新忙碌状态，操作锁仍阻止并发恢复；这两项分别通过脚本等待方式和真实导入流程验证。
